node_modules/
.env
data/*
!data/student_records/
*.log

# Personal/example spreadsheets dropped into the project folder for testing —
# not part of the app itself (the reference template stays tracked).
Render copy.xlsx
